# S-7 설문 기록 탭 워크플로우

> 담당: 정성오  
> 관련 todo: `todo_260417Fri.md` > Phase 3 > S-7  
> 작성일: 2026-05-24 (Sun)

---

## 📋 전체 흐름 요약

```
Survey 결과 저장/조회 API 확인
  → MyPage에 설문 기록 탭 UI 추가
    → 설문 결과 없을 때/있을 때 분기 렌더
      → Survey 페이지에서 저장 트리거 연결
        → MatchResult에서 기록 재확인 링크 제공
```

---

## 📌 현재 상태 파악 (작업 전)

- `feature-spec_260330Mon.md` — S-7: 설문 기록 탭 (마이페이지에서 설문 결과 확인)
- `ERD_260331Tue.md` — `SURVEY_RESULTS` 테이블 존재, **S-7은 이 테이블로 커버**
- `frontend/src/pages/Survey.jsx` — 설문 5문항 UI와 매칭 API 호출 구현됨
- `frontend/src/pages/MatchResult.jsx` — 설문 결과 페이지 존재 (매칭 결과 렌더)
- `frontend/src/components/Navbar.jsx` — `/survey` 링크 존재
- 백엔드 `SURVEY_RESULTS` 저장/조회 API 존재 여부는 확인 필요 (없으면 간단 CRUD 추가)

---

## ✅ Task 목록 (todo 형식)

### 1단계: API/DB 상태 확인

- [ ] `backend`에 `SURVEY_RESULTS` 저장/조회 API가 있는지 확인
  - [ ] 없으면 최소 API 2개 설계
    - [ ] `POST /api/surveys` — 설문 결과 저장(1인 1행 upsert)
    - [ ] `GET /api/surveys/me` — 내 설문 결과 조회
  - [ ] 있으면 프론트에서 호출할 경로/응답 필드 정리

---

### 2단계: 마이페이지 설문 기록 탭 UI 추가

- [ ] `MyPage.jsx`(또는 마이페이지 루트 파일) 위치 확인
- [ ] 탭 구조에 "설문 기록" 탭 추가
- [ ] 탭 클릭 시 설문 결과 조회 API 호출
- [ ] 응답 필드 UI 매핑
  - [ ] `q_upkind`, `q_sex_cd`, `q_neuter_yn`, `q_upr_cd`, `q_state` 표시
  - [ ] 코드값을 한글 라벨로 변환
    - [ ] `417000/422400/429900` → 개/고양이/기타
    - [ ] `M/F/Q` → 수컷/암컷/미상
    - [ ] `Y/N/U` → 중성화/미중성/미상
    - [ ] `notice/protect/''` → 공고중/보호중/상관없음

---

### 3단계: 설문 결과 저장 트리거 연결

- [ ] `Survey.jsx` 또는 `match.js`에서 매칭 성공 시 저장 API 호출
  - [ ] 기존 `matchQuiz` 성공 후 `POST /api/surveys` 추가 호출
  - [ ] 실패 시 설문 결과 저장만 실패했다는 메시지 노출(매칭 결과는 유지)

---

### 4단계: 빈 상태/예외 처리

- [ ] 설문 기록이 없을 때 안내 문구
  - [ ] "아직 설문 기록이 없습니다" + 설문 링크 버튼
- [ ] API 실패 시 에러 메시지
- [ ] 로딩 상태 스켈레톤 또는 텍스트 표시

---

### 5단계: 동작 확인 (로컬 테스트)

- [ ] 설문 제출 → 매칭 결과 정상 노출 확인
- [ ] 마이페이지 설문 기록 탭에서 값 표시 확인
- [ ] 설문 기록 없을 때 빈 상태 화면 확인
- [ ] 새 설문 제출 시 기록이 갱신되는지 확인

---

## 📁 최종 파일 변경 목록 (예상)

| 상태 | 파일 경로 | 설명 |
|------|-----------|------|
| 🔧 수정 | `frontend/src/pages/Survey.jsx` | 설문 저장 API 호출 추가 |
| 🔧 수정 | `frontend/src/pages/MatchResult.jsx` | 설문 기록 이동 링크 추가 (선택) |
| 🔧 수정 | `frontend/src/pages/MyPage.jsx` | 설문 기록 탭 UI/로직 추가 |
| 🔧 수정 | `frontend/src/api/survey.js` | 설문 저장/조회 API 함수 (없으면 신규) |
| 🔧 수정 | `backend/...` | 설문 저장/조회 API (없으면 신규) |

---

## ⚠️ 주의사항 & 참고

- S-7은 별도 테이블 추가 없이 `SURVEY_RESULTS` 1인 1행(upsert)로 처리
- 설문 결과 저장 실패가 매칭 결과를 막으면 UX 나빠짐 → 저장 실패는 경고만
- 설문 기록 탭은 "결과 조회"가 핵심이므로 UI는 단순 카드/리스트 형태 권장
- `.env` 관련 작업 필요 시 사용자가 직접 처리

---

## 🔗 관련 문서

- `doc/common/updatable/todo_260417Fri.md`
- `doc/common/updatable/feature-spec_260330Mon.md`
- `doc/common/updatable/tech-stack_260414Tue.md`
- `doc/common/updatable/ERD_260331Tue.md`
- `doc/common/constant/AGENTS.md`
