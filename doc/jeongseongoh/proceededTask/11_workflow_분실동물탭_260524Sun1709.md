# S-2 분실동물 탭 워크플로우

> 담당: 정성오  
> 관련 todo: `todo_260417Fri.md` > Phase 3 > S-2  
> 작성일: 2026-05-24 (Sun)

---

## 📋 전체 흐름 요약

```
LostAnimalPage.jsx 신규 생성
  → App.jsx에 /lost-animals 라우트 추가
    → Navbar에 분실동물 탭 링크 추가
      → fetchLostAnimals() API 함수 작성 (animals.js 재활용)
        → Mock JSON (public/mock/lost_animals.json) 준비
          → AnimalFeedPage 구조 거의 그대로 재사용해서 렌더링
```

---

## 📌 현재 상태 파악 (작업 전)

- `AnimalFeedPage.jsx` — 구조동물 피드 완성. 필터 로직(`applyFilter`), `FilterBar`, `AnimalCard` 이미 사용 중
  - **S-2는 이 파일 구조를 거의 그대로 복사해서 사용 가능** (공수 낮음)
- `FilterBar.jsx` — 공통 필터 컴포넌트. 재사용 가능
- `AnimalCard.jsx` — 공통 카드 컴포넌트. 재사용 가능
- `api/animals.js` — `fetchAnimals()`, `fetchSido()`, `fetchSigungu()`, `fetchShelters()` 존재
  - 분실동물용 `fetchLostAnimals()` 함수를 여기에 추가하면 됨
- `App.jsx` — 현재 `/animals` 라우트만 있음. `/lost-animals` 라우트 추가 필요
- `Navbar.jsx` — 현재 탭 목록에 분실동물 없음. 탭 링크 추가 필요
- `public/mock/` — `animals.json`, `codes_sido.json` 등 있음. `lost_animals.json` 없음 → 새로 만들어야 함

---

## ✅ Task 목록

### 1단계: 기존 코드 구조 재확인

- [x] `AnimalFeedPage.jsx` 코드 다시 읽기 — `applyFilter` 함수, `DEFAULT_FILTERS`, JSX 구조 확인
- [x] `FilterBar.jsx` props 확인 — `sidoList`, `sigunguList`, `shelterList`, `filters`, `onChange`, `disabledSigungu`, `disabledShelter`
- [x] `AnimalCard.jsx` props 확인 — `animal` 객체 필드명 확인 (`id`, `kind`, `gender`, `status`, `shelterName`, `jurisdiction` 등)
- [x] `Navbar.jsx` 확인 — 탭 링크 목록 구조 파악 (어디에 추가해야 하는지)
- [x] `public/mock/animals.json` 구조 확인 — 분실동물 Mock JSON 만들 때 필드 형식 참고용

---

### 2단계: Mock JSON 준비 (`public/mock/lost_animals.json`)

> **핵심 목표**: 분실동물 더미 데이터 JSON 파일 생성 (백엔드 연동 전 프론트 개발용)

- [x] `public/mock/lost_animals.json` 신규 생성
  - `animals.json`과 동일한 필드 구조 사용 (재사용성 위해)
    ```json
    {
      "data": {
        "items": [
          {
            "id": "lost_001",
            "kind": "개",
            "gender": "M",
            "isNeutered": "Y",
            "status": "분실",
            "jurisdiction": "서울특별시 강남구",
            "shelterName": "강남구 동물보호센터",
            "photoUrl": "",
            "happenDt": "2026-05-20",
            "happenPlace": "서울 강남구 역삼동 일대",
            "specialMark": "갈색 소형견, 목줄 없음"
          }
        ]
      }
    }
    ```
  - 더미 데이터 최소 5~10개 정도 작성 (종/성별/지역 다양하게)
  - ⚠️ `status` 필드 값: `"분실"` (구조동물은 `"공고중"` / `"보호중"`)

---

### 3단계: API 함수 추가 (`api/animals.js`)

> **핵심 목표**: 분실동물 Mock JSON을 불러오는 함수 추가

- [x] `api/animals.js` 하단에 `fetchLostAnimals()` 함수 추가
  ```js
  // 분실동물 목록 불러오기 (Mock JSON)
  async function fetchLostAnimals() {
    const response = await fetch('/mock/lost_animals.json');
    if (!response.ok) {
      throw new Error('분실동물 데이터를 불러오지 못했습니다.');
    }
    const data = await response.json();
    return data?.data?.items || [];
  }
  ```
- [x] `export` 목록에 `fetchLostAnimals` 추가
  ```js
  export { fetchAnimals, fetchSido, fetchSigungu, fetchShelters, fetchLostAnimals };
  ```

---

### 4단계: LostAnimalPage.jsx 신규 생성

> **핵심 목표**: `AnimalFeedPage.jsx`를 기반으로 분실동물 페이지 생성 (구조 거의 동일)

- [x] `frontend/src/pages/LostAnimalPage.jsx` 신규 생성
  - `AnimalFeedPage.jsx` 구조를 그대로 복사 후 아래 항목만 수정
  - **변경 필요 항목**:
    - `import` 에서 `fetchAnimals` → `fetchLostAnimals`로 변경
    - `init()` 안에서 `fetchAnimals()` → `fetchLostAnimals()` 호출로 변경
    - `<h1>` 텍스트: `"구조동물 피드"` → `"분실동물 탭"`
    - `<p>` 설명 텍스트: `"분실된 반려동물을 지역과 조건으로 찾아보세요."`
    - `총 {filteredAnimals.length}마리` → `총 {filteredAnimals.length}건`
  - **유지할 항목**:
    - `DEFAULT_FILTERS` 상수 (그대로)
    - `applyFilter(animals, filters)` 함수 (그대로)
    - `FilterBar` 컴포넌트 사용 (그대로)
    - `AnimalCard` 컴포넌트 사용 (그대로)
    - 300ms debounce 로직 (그대로)
    - 연쇄 필터 (`handleCascadeChange`) 로직 (그대로)
  - 코드 내 주석 한국어로 작성 (교수님 감점 방지)

---

### 5단계: App.jsx 라우트 추가

- [x] `App.jsx` 상단 import에 `LostAnimalPage` 추가
  ```jsx
  import LostAnimalPage from './pages/LostAnimalPage.jsx';
  ```
- [x] `<Route>` 목록에 `/lost-animals` 경로 추가
  ```jsx
  <Route path="lost-animals" element={<LostAnimalPage />} /> {/*분실동물 탭*/}
  ```

---

### 6단계: Navbar에 탭 링크 추가

- [x] `Navbar.jsx` 열어서 현재 탭 목록 위치 확인
- [x] 분실동물 탭 링크 추가
  ```jsx
  <Link to="/lost-animals">분실동물</Link>
  ```
  - 기존 구조동물 탭 링크 (`/animals`) 바로 옆에 배치
  - 스타일: 기존 탭 링크와 동일한 Tailwind 클래스 사용

---

### 7단계: 동작 확인 (로컬 테스트)

- [ ] `npm run dev` 실행 후 `/lost-animals` 경로 접속 확인
- [ ] Mock JSON 데이터가 카드 형태로 렌더링되는지 확인
- [ ] 필터 드롭다운(시도/시군구/보호소/종/성별 등) 동작 확인
- [ ] 조건에 맞는 동물이 없을 때 안내 문구 표시 확인
- [ ] Navbar 탭 클릭으로 `/lost-animals` 이동 동작 확인

---

## 📁 최종 파일 변경 목록

| 상태 | 파일 경로 | 설명 |
|------|-----------|------|
| 🆕 신규 | `frontend/src/pages/LostAnimalPage.jsx` | 분실동물 탭 페이지 (AnimalFeedPage 재활용) |
| 🆕 신규 | `public/mock/lost_animals.json` | 분실동물 더미 데이터 |
| 🔧 수정 | `frontend/src/api/animals.js` | `fetchLostAnimals()` 함수 추가 |
| 🔧 수정 | `frontend/src/App.jsx` | `/lost-animals` 라우트 추가 |
| 🔧 수정 | `frontend/src/components/Navbar.jsx` | 분실동물 탭 링크 추가 |

---

## ⚠️ 주의사항 & 참고

- S-2는 `feature-spec`에서 "F-01/02 코드 거의 재사용 → 공수 낮음"으로 명시됨 → 새 로직 최소화
- Mock JSON의 필드명은 `AnimalCard.jsx`가 기대하는 필드명과 반드시 일치해야 함
  - `AnimalCard` props 구조 먼저 확인 후 Mock 데이터 작성 (4단계 전에 1단계 완료 필수)
- `lost_animals.json` `status` 필드는 `"분실"` 값으로 (구조동물과 구분)
- Phase 2 연동 시: `fetchLostAnimals()`의 fetch URL만 `/mock/lost_animals.json` → `/api/lost-animals` 로 교체하면 됨 (그 외 코드 변경 최소)
- `.env` 관련 작업 필요 시 사용자가 직접 처리

---

## 🔗 관련 문서

- [todo_260417Fri.md](../../common/updatable/todo_260417Fri.md)
- [feature-spec_260330Mon.md](../../common/updatable/feature-spec_260330Mon.md)
- [tech-stack_260414Tue.md](../../common/updatable/tech-stack_260414Tue.md)
- [AGENTS.md](../../common/constant/AGENTS.md)
